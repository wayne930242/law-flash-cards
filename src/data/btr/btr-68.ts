import { TypeData, TypeFlashCard } from '../interface'

const id = 'btr-68'

const name = '技規 #68 雜項防火'

const head: TypeData[][] = [
]

const data: TypeData[][] = [
  ['廣告牌(塔)、裝飾物(塔)', '[3]m 以上', '[不燃材料]'],
  ['^', '[屋頂上]', '^'],
]

export const Flashcard: TypeFlashCard = {
  id,
  name,
  head,
  data,
}
